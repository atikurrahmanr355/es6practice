const firstName = "Tanvir";
const lastName = "Ahmed";

const fullName = firstName + ' ' + lastName;
console.log(fullName);
//using template tag
const fullNames = `${firstName} ${lastName} is a good boy!`;
console.log(fullName);